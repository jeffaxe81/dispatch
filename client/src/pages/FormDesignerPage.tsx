import DashboardLayout from"@/components/DashboardLayout";import FormDesigner from"@/components/forms/FormDesigner";import{Button}from"@/components/ui/button";import type{FormSchemaDefinition}from"@shared/forms";import{ArrowLeft,Save,Send}from"lucide-react";import{useState}from"react";import{useLocation}from"wouter";
export type FormDesignerPageModel={formId:number;code:string;name:string;versionId:number;version:number;status:"draft"|"published"|"retired";definition:FormSchemaDefinition};export type FormDesignerPageProps={initial?:FormDesignerPageModel;onSave?:(input:{versionId:number;definition:FormSchemaDefinition})=>void|Promise<void>;onPublish?:(versionId:number)=>void|Promise<void>;onCreateNewVersion?:(versionId:number)=>void|Promise<void>};
const fallback:FormDesignerPageModel={formId:0,code:"NOVO",name:"Novo formulário",versionId:0,version:1,status:"draft",definition:{schemaVersion:1,title:"Novo formulário",fields:[]}};
export default function FormDesignerPage({initial=fallback,onSave=()=>{},onPublish=()=>{},onCreateNewVersion=()=>{}}:FormDesignerPageProps){const[,navigate]=useLocation(),[definition,setDefinition]=useState(initial.definition),published=initial.status==="published";return <DashboardLayout><div className="mx-auto max-w-[1600px] space-y-6 pb-8"><header className="flex flex-col gap-4 rounded-2xl bg-gradient-to-r from-slate-950 to-sky-900 px-6 py-6 text-white md:flex-row md:items-end md:justify-between"><div><button type="button" onClick={()=>navigate("/formularios")} className="inline-flex items-center gap-1 text-xs uppercase tracking-[.18em] text-sky-100"><ArrowLeft className="h-3.5 w-3.5"/>Formulários</button><h1 className="mt-3 text-3xl font-semibold">{initial.name}</h1><p className="mt-1 text-sm text-sky-100">{initial.code} · versão {initial.version} · {published?"Publicada":"Rascunho"}</p></div><div className="flex flex-wrap gap-2">{!published&&<><Button variant="outline" className="border-white/30 bg-white/10 text-white hover:bg-white/20 hover:text-white" onClick={()=>onSave({versionId:initial.versionId,definition})}><Save className="mr-2 h-4 w-4"/>Salvar rascunho</Button><Button className="bg-white text-slate-950 hover:bg-sky-50" onClick={()=>onPublish(initial.versionId)}><Send className="mr-2 h-4 w-4"/>Publicar versão</Button></>}</div></header><FormDesigner definition={definition} onChange={setDefinition} published={published} onCreateNewVersion={()=>onCreateNewVersion(initial.versionId)}/></div></DashboardLayout>}
