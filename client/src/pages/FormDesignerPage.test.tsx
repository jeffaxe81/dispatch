// @vitest-environment jsdom
import{fireEvent,render,screen}from"@testing-library/react";import React from"react";import{describe,expect,it,vi}from"vitest";import FormDesignerPage from"./FormDesignerPage";
vi.mock("wouter",()=>({useRoute:()=>[true,{id:"3"}],useLocation:()=>["/formularios/3",vi.fn()]}));
describe("D-008 FormDesignerPage",()=>{it("renderiza designer em rascunho com ações de salvar e publicar",()=>{render(<FormDesignerPage initial={{formId:3,code:"VISTORIA",name:"Vistoria",versionId:5,version:1,status:"draft",definition:{schemaVersion:1,title:"Vistoria",fields:[]}}} onSave={vi.fn()} onPublish={vi.fn()} onCreateNewVersion={vi.fn()}/>);expect(screen.getByRole("heading",{name:/Vistoria/})).toBeTruthy();expect(screen.getByRole("button",{name:/Salvar rascunho/i})).toBeTruthy();expect(screen.getByRole("button",{name:/Publicar versão/i})).toBeTruthy()});it("publicada fica imutável e oferece criação de nova versão",()=>{const createNewVersion=vi.fn();render(<FormDesignerPage initial={{formId:3,code:"VISTORIA",name:"Vistoria",versionId:5,version:1,status:"published",definition:{schemaVersion:1,title:"Vistoria",fields:[]}}} onSave={vi.fn()} onPublish={vi.fn()} onCreateNewVersion={createNewVersion}/>);expect(screen.queryByRole("button",{name:/Salvar rascunho/i})).toBeNull();expect(screen.getByRole("button",{name:/Criar nova versão/i})).toBeTruthy();fireEvent.click(screen.getByRole("button",{name:/Criar nova versão/i}));expect(createNewVersion).toHaveBeenCalledWith(5)});});