import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const source=readFileSync(new URL("./FormDesignerPage.tsx",import.meta.url),"utf8");describe("D-008 FormDesignerPage runtime wiring",()=>{it("carrega e persiste versão pelo namespace forms",()=>{expect(source).toContain("trpc.forms.get.useQuery");expect(source).toContain("trpc.forms.updateDraft.useMutation");expect(source).toContain("trpc.forms.publish.useMutation");expect(source).toContain("trpc.forms.createNewVersion.useMutation")});it("export default é tela sem props obrigatórias",()=>{expect(source).toContain("export default function FormDesignerPageScreen()");});});