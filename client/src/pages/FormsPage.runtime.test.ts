import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const source=readFileSync(new URL("./FormsPage.tsx",import.meta.url),"utf8");describe("D-008 FormsPage runtime wiring",()=>{it("usa autorização e dados do namespace forms",()=>{expect(source).toContain("trpc.forms.capabilities.useQuery");expect(source).toContain("trpc.forms.list.useQuery");expect(source).toContain("trpc.forms.createDraft.useMutation")});it("export default não exige props operacionais do App",()=>{expect(source).toContain("export default function FormsPageScreen()");});});