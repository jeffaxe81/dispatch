// @vitest-environment jsdom
import{fireEvent,render,screen}from"@testing-library/react";import React from"react";import{describe,expect,it,vi}from"vitest";import{FormRenderer}from"./FormRenderer";
const definition={schemaVersion:1 as const,title:"Aceite",fields:[{id:"sig",key:"assinatura",type:"simple_signature" as const,label:"Assinatura",required:true}]};
describe("D-008 simple signature",()=>{it("usa superfície de assinatura na tela em vez de seletor de arquivo",()=>{render(<FormRenderer definition={definition} values={{}} onChange={()=>{}} onAttachmentSelected={()=>{}}/>);expect(screen.getByRole("img",{name:/área de assinatura simples/i})).toBeTruthy();expect(screen.getByText(/não ICP-Brasil/i)).toBeTruthy();expect(document.querySelector('input[type="file"]')).toBeNull();});it("confirma desenho como PNG para o fluxo de anexos",async()=>{const selected=vi.fn();vi.spyOn(HTMLCanvasElement.prototype,"getContext").mockReturnValue({beginPath:vi.fn(),moveTo:vi.fn(),lineTo:vi.fn(),stroke:vi.fn(),clearRect:vi.fn()}as any);vi.spyOn(HTMLCanvasElement.prototype,"toBlob").mockImplementation(callback=>callback(new Blob(["sig"],{type:"image/png"})));render(<FormRenderer definition={definition} values={{}} onChange={()=>{}} onAttachmentSelected={selected}/>);const pad=screen.getByRole("img",{name:/área de assinatura simples/i});fireEvent.pointerDown(pad,{clientX:1,clientY:1,pointerId:1});fireEvent.pointerMove(pad,{clientX:10,clientY:10,pointerId:1});fireEvent.pointerUp(pad,{pointerId:1});fireEvent.click(screen.getByRole("button",{name:/confirmar assinatura/i}));await vi.waitFor(()=>expect(selected).toHaveBeenCalled());const[key,file,kind]=selected.mock.calls[0];expect(key).toBe("assinatura");expect(kind).toBe("simple_signature");expect(file).toBeInstanceOf(File);expect(file.type).toBe("image/png");});});