import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("D-008 forms UI routes",()=>{it("registra gestão e designer sem alterar fallback",()=>{const source=readFileSync(new URL("./App.tsx",import.meta.url),"utf8");expect(source).toMatch(/FormsPage/);expect(source).toMatch(/FormDesignerPage/);expect(source).toMatch(/path=\{"\/formularios"\}/);expect(source).toMatch(/path=\{"\/formularios\/:id"\}/);expect(source).toMatch(/<Route component=\{NotFound\}/)});});