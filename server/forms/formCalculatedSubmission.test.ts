import{describe,expect,it,vi}from"vitest";import{createFormService,type FormServicePorts}from"./formService";const definition={schemaVersion:1 as const,title:"Cálculo",fields:[{id:"qty",key:"qty",label:"Quantidade",type:"number" as const,required:true},{id:"copy",key:"copy",label:"Cópia",type:"calculated" as const,required:true,expression:"qty"}]};function ports():FormServicePorts{return{repository:{getTemplate:vi.fn(),listTemplates:vi.fn(async()=>[]),getVersion:vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"published" as const,definition})),listVersions:vi.fn(async()=>[]),getSubmission:vi.fn(),createDraft:vi.fn(),createVersion:vi.fn(),saveDraft:vi.fn(),publishVersion:vi.fn(),activateForm:vi.fn(),disableForm:vi.fn(),createSubmission:vi.fn(async input=>({id:21,...input})),finalizeSubmission:vi.fn(),appendRevision:vi.fn(),bindForm:vi.fn(),createAttachment:vi.fn(),listBindings:vi.fn(async()=>[]),listSubmissionsForIncident:vi.fn(async()=>[])},audit:{append:vi.fn(async()=>undefined)},events:{append:vi.fn(async()=>undefined)}}}describe("D-008 calculated submission",()=>{it("materializa referência direta antes de validar e persistir",async()=>{const p=ports();await createFormService(7,p).submitForm({formId:3,formVersionId:5,answers:{qty:4},actorUserId:9,now:new Date()});expect(p.repository.createSubmission).toHaveBeenCalledWith(expect.objectContaining({answers:{qty:4,copy:4},status:"submitted"}))});});