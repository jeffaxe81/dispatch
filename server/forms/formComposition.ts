import{createFormRepository,type FormRepositoryAdapter}from"./formRepository";import{createFormService,type FormAuditEntry,type StoredFormAttachment}from"./formService";import type{FormAttachmentKind}from"./formAttachments";
export type FormAttachmentStoreInput={tenantId:number;submissionId:number;revisionId?:number|null;fieldKey:string;kind:FormAttachmentKind;fileName:string;mimeType:string;bytes:Buffer};
export type FormsCompositionInput={tenantId:number;persistence:FormRepositoryAdapter;audit:{append(entry:FormAuditEntry):Promise<void>};attachmentStore(input:FormAttachmentStoreInput):Promise<StoredFormAttachment>;};
export function createFormsComposition(input:FormsCompositionInput){if(!Number.isInteger(input.tenantId)||input.tenantId<=0)throw new Error("Tenant válido é obrigatório para compor o módulo de formulários.");const repository=createFormRepository(input.tenantId,input.persistence);const events={append:async(event:Parameters<typeof repository.appendDomainEvent>[0]&{tenantId:number})=>{if(event.tenantId!==input.tenantId)throw new Error("Evento de formulário pertence a outro tenant.");const{tenantId:_serviceTenant,...outboxEvent}=event;await repository.appendDomainEvent(outboxEvent);}};const audit={append:async(entry:FormAuditEntry)=>{if(entry.tenantId!==input.tenantId)throw new Error("Auditoria de formulário pertence a outro tenant.");await input.audit.append(entry);}};const attachments={store:async(attachment:FormAttachmentStoreInput)=>{if(attachment.tenantId!==input.tenantId)throw new Error("Anexo de formulário pertence a outro tenant.");return input.attachmentStore(attachment);}};const service=createFormService(input.tenantId,{repository,audit,events,attachments});return{tenantId:input.tenantId,repository,service,audit,events,attachments};}
