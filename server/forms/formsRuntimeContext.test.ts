import{describe,expect,it,vi}from"vitest";import{createFormsRuntimeContextFactory}from"./formsRuntimeContext";const user={id:9,teamId:4,active:true,operationalRole:"supervisor"}as any;describe("D-008 runtime context",()=>{it("resolve tenant, service, permissões e escopos operacionais",async()=>{const createService=vi.fn(()=>({list:vi.fn()})),assertIncidentScope=vi.fn(async()=>undefined),assertSubmissionScope=vi.fn(async()=>undefined),factory=createFormsRuntimeContextFactory({findTeamOrganizationId:vi.fn(async()=>7),getAccessSnapshot:vi.fn(async()=>({assignments:[{roleCode:"forms_supervisor"}],permissions:new Set(["forms.view"])})),createService,assertIncidentScope,assertSubmissionScope});const ctx=await factory({user});expect(ctx.tenantId).toBe(7);expect(createService).toHaveBeenCalledWith(7);await expect(ctx.hasPermission("forms.view")).resolves.toBe(true);await expect(ctx.hasPermission("forms.fill")).resolves.toBe(false);await ctx.assertIncidentScope("88");expect(assertIncidentScope).toHaveBeenCalledWith(user,"88");await ctx.assertSubmissionScope(21);expect(assertSubmissionScope).toHaveBeenCalledWith(user,7,21)});it("usa fallback legado de formulários somente quando não existem assignments dinâmicos",async()=>{const factory=createFormsRuntimeContextFactory({findTeamOrganizationId:vi.fn(async()=>7),getAccessSnapshot:vi.fn(async()=>({assignments:[],permissions:new Set()})),createService:vi.fn(()=>({})),assertIncidentScope:vi.fn(),assertSubmissionScope:vi.fn()});const ctx=await factory({user});await expect(ctx.hasPermission("forms.fill")).resolves.toBe(true);await expect(ctx.hasPermission("forms.publish")).resolves.toBe(false)});it("falha fechado sem usuário ou tenant",async()=>{const factory=createFormsRuntimeContextFactory({findTeamOrganizationId:vi.fn(async()=>null),getAccessSnapshot:vi.fn(),createService:vi.fn(),assertIncidentScope:vi.fn(),assertSubmissionScope:vi.fn()});await expect(factory({user:null})).rejects.toThrow(/autenticado/i);await expect(factory({user})).rejects.toThrow(/tenant|organização/i)});});