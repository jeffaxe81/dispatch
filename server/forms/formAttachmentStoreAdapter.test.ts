import{describe,expect,it,vi}from"vitest";import{createFormAttachmentStore}from"./formAttachmentStoreAdapter";
const input={tenantId:7,submissionId:21,fieldKey:"foto",kind:"image" as const,fileName:"foto.png",mimeType:"image/png",bytes:Buffer.from("png")};
describe("D-008 attachment store adapter",()=>{it("usa storage existente e retorna metadados com SHA-256",async()=>{const storagePut=vi.fn(async()=>({key:"stored",url:"/manus-storage/stored"}));const store=createFormAttachmentStore({storagePut});const result=await store(input);expect(storagePut).toHaveBeenCalledWith(expect.stringContaining("tenants/7/forms/submissions/21/current/foto/"),input.bytes,"image/png");expect(result.sha256).toMatch(/^[a-f0-9]{64}$/);expect(result.malwareScan.status).toBe("not_configured");});it("bloqueia arquivo antes do upload quando scanner reprova",async()=>{const storagePut=vi.fn(),scanner={scan:vi.fn(async()=>({status:"blocked" as const,engine:"test"}))};const store=createFormAttachmentStore({storagePut,malwareScanner:scanner});await expect(store(input)).rejects.toThrow(/antimalware/i);expect(storagePut).not.toHaveBeenCalled();});});