import{createHash}from"node:crypto";import{and,eq}from"drizzle-orm";import{formAttachments,formBindings,formDomainEvents,formSubmissionRevisions,formSubmissions,formTemplates,formVersions}from"../../drizzle/formsSchema";import{getDb}from"../db";import type{FormRepositoryAdapter}from"./formRepository";type DbProvider={getDb:typeof getDb};function answersHash(answers:unknown){return createHash("sha256").update(JSON.stringify(answers)).digest("hex")}
export function createFormRepositoryDbAdapter(provider:DbProvider={getDb}):FormRepositoryAdapter{async function db(){const value=await provider.getDb();if(!value)throw new Error("Banco de dados indisponível para formulários.");return value}return{async getTemplate(input){const d=await db(),rows=await d.select().from(formTemplates).where(and(eq(formTemplates.tenantId,input.tenantId),eq(formTemplates.id,input.id))).limit(1);return rows[0]as any??null},async listTemplates(input){const d=await db();return d.select().from(formTemplates).where(eq(formTemplates.tenantId,input.tenantId))as any},async getVersion(input){const d=await db(),rows=await d.select().from(formVersions).where(and(eq(formVersions.tenantId,input.tenantId),eq(formVersions.id,input.id))).limit(1);return rows[0]as any??null},async listVersions(input){const d=await db();return d.select().from(formVersions).where(and(eq(formVersions.tenantId,input.tenantId),eq(formVersions.formId,input.formId)))as any},async getSubmission(input){const d=await db(),rows=await d.select().from(formSubmissions).where(and(eq(formSubmissions.tenantId,input.tenantId),eq(formSubmissions.id,input.id))).limit(1),row=rows[0]as any;if(!row)return null;const revisions=await d.select().from(formSubmissionRevisions).where(and(eq(formSubmissionRevisions.tenantId,input.tenantId),eq(formSubmissionRevisions.submissionId,input.id)))as any[];return{...row,revision:revisions.reduce((max,item)=>Math.max(max,Number(item.revision??0)),1)}},async createDraft(input){const d=await db(),result=await d.insert(formTemplates).values({tenantId:input.tenantId,code:input.code,name:input.name,createdByUserId:input.createdByUserId}).$returningId();return result[0]??result},async saveDraft(input){const d=await db();await d.update(formVersions).set({definition:input.definition}).where(and(eq(formVersions.tenantId,input.tenantId),eq(formVersions.id,input.versionId),eq(formVersions.status,"draft")));return{versionId:input.versionId}},async publishVersion(input){const d=await db();await d.update(formVersions).set({status:"published",publishedByUserId:input.actorUserId,publishedAt:input.publishedAt}).where(and(eq(formVersions.tenantId,input.tenantId),eq(formVersions.id,input.versionId),eq(formVersions.status,"draft")));return{versionId:input.versionId,status:"published"}},async activateForm(input){const d=await db();await d.update(formTemplates).set({status:"active"}).where(and(eq(formTemplates.tenantId,input.tenantId),eq(formTemplates.id,input.formId)));return{formId:input.formId,status:"active"}},async disableForm(input){const d=await db();await d.update(formTemplates).set({status:"disabled"}).where(and(eq(formTemplates.tenantId,input.tenantId),eq(formTemplates.id,input.formId)));return{formId:input.formId,status:"disabled"}},async createBinding(input){const d=await db(),result=await d.insert(formBindings).values(input).$returningId();return result[0]??result},async createSubmission(input){const d=await db(),submitted=input.status==="submitted",result=await d.insert(formSubmissions).values({...input,submittedByUserId:submitted?input.createdByUserId:null,submittedAt:submitted?(input.submittedAt??new Date()):null}).$returningId();return result[0]??result},async appendRevision(input){const d=await db(),afterHash=answersHash(input.answers),result=await d.insert(formSubmissionRevisions).values({tenantId:input.tenantId,submissionId:input.submissionId,revision:input.revision,answers:input.answers,reason:input.reason,actorUserId:input.actorUserId,afterHash}).$returningId();await d.update(formSubmissions).set({status:input.submissionStatus,answers:input.answers}).where(and(eq(formSubmissions.tenantId,input.tenantId),eq(formSubmissions.id,input.submissionId)));return result[0]??result},async createAttachment(input){const d=await db(),result=await d.insert(formAttachments).values(input).$returningId();return result[0]??result},async appendDomainEvent(input){const d=await db(),result=await d.insert(formDomainEvents).values(input).$returningId();return result[0]??result},async listBindings(input){const d=await db(),conditions:any[]=[eq(formBindings.tenantId,input.tenantId)];if(input.contextType)conditions.push(eq(formBindings.contextType,input.contextType));if(input.contextId)conditions.push(eq(formBindings.contextId,input.contextId));return d.select().from(formBindings).where(and(...conditions))as any},async listSubmissionsForIncident(input){const d=await db();return d.select().from(formSubmissions).where(and(eq(formSubmissions.tenantId,input.tenantId),eq(formSubmissions.contextType,"incident"),eq(formSubmissions.contextId,input.incidentId)))as any}}}