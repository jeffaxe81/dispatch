import type{User}from"../../drizzle/schema";import type{FormPermission}from"./formAccess";export type FormsContextUser=Pick<User,"id"|"teamId">;export type FormsContextPorts={resolveTenantId(user:{userId:number;teamId:number|null}):Promise<number>;hasPermission(user:User,permission:FormPermission):Promise<boolean>;assertIncidentScope(user:User,incidentId:string):Promise<void>;assertSubmissionScope(user:User,tenantId:number,submissionId:number):Promise<void>;createService(tenantId:number):Record<string,(...args:any[])=>any>;};export async function createFormsRequestContext(user:User|null,ports:FormsContextPorts){if(!user)throw new Error("Usuário autenticado é obrigatório para acessar formulários.");const tenantId=await ports.resolveTenantId({userId:user.id,teamId:user.teamId});if(!Number.isInteger(tenantId)||tenantId<=0)throw new Error("Tenant válido é obrigatório para acessar formulários.");const service=ports.createService(tenantId);return{tenantId,userId:user.id,service,hasPermission:(permission:FormPermission)=>ports.hasPermission(user,permission),assertIncidentScope:(incidentId:string)=>ports.assertIncidentScope(user,incidentId),assertSubmissionScope:(submissionId:number)=>ports.assertSubmissionScope(user,tenantId,submissionId)}}