import{describe,expect,it}from"vitest";import{rootRouter}from"../rootRouter";describe("application root router with D-008",()=>{it("registra namespace forms sem remover módulos existentes",()=>{const procedures=rootRouter._def.procedures;expect(procedures["forms.capabilities"]).toBeDefined();expect(procedures["forms.list"]).toBeDefined();expect(procedures["forms.forIncident"]).toBeDefined();expect(procedures["forms.startSubmission"]).toBeDefined();expect(procedures["forms.submit"]).toBeDefined();expect(procedures["forms.uploadAttachment"]).toBeDefined();expect(procedures["dispatch.rankEligibleCandidates"]).toBeDefined();expect(procedures["workShiftSchedules.list"]).toBeDefined()})});