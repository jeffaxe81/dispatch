import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("D-008 root router registration",()=>{it("registra namespace forms com runtime context autenticado",()=>{const source=readFileSync(new URL("../routers.ts",import.meta.url),"utf8");expect(source).toMatch(/createFormsTrpcRouter/);expect(source).toMatch(/formsRuntimeContextFactory/);expect(source).toMatch(/forms:\s*createFormsTrpcRouter\(formsRuntimeContextFactory\)/)});});