import { describe, expect, it, vi } from "vitest";
import { createFormsRequestContext } from "./formsTrpcContext";
describe("D-008 tRPC forms context",()=>{
it("resolve tenant no servidor, cria service tenant-bound e avalia RBAC autenticado",async()=>{const resolveTenantId=vi.fn(async()=>77);const hasPermission=vi.fn(async(_user:any,permission:string)=>permission==="forms.view");const service={list:vi.fn()};const createService=vi.fn((tenantId:number)=>{expect(tenantId).toBe(77);return service;});const ctx=await createFormsRequestContext({id:9,teamId:3}as any,{resolveTenantId,hasPermission,createService});expect(ctx.tenantId).toBe(77);expect(ctx.userId).toBe(9);expect(ctx.service).toBe(service);expect(createService).toHaveBeenCalledWith(77);await expect(ctx.hasPermission("forms.view")).resolves.toBe(true);expect(resolveTenantId).toHaveBeenCalledWith(expect.objectContaining({userId:9,teamId:3}));});
it("não cria service antes de resolver tenant",async()=>{const order:string[]=[];await createFormsRequestContext({id:9,teamId:3}as any,{resolveTenantId:vi.fn(async()=>{order.push("tenant");return 77;}),hasPermission:vi.fn(async()=>true),createService:vi.fn(()=>{order.push("service");return{};})});expect(order).toEqual(["tenant","service"]);});
it("rejeita requisição sem usuário autenticado sem compor service",async()=>{const createService=vi.fn();await expect(createFormsRequestContext(null,{resolveTenantId:vi.fn(),hasPermission:vi.fn(),createService})).rejects.toThrow(/autentic/i);expect(createService).not.toHaveBeenCalled();});
it("falha fechado se resolver tenant inválido",async()=>{const createService=vi.fn();await expect(createFormsRequestContext({id:9,teamId:3}as any,{resolveTenantId:vi.fn(async()=>0),hasPermission:vi.fn(async()=>true),createService})).rejects.toThrow(/tenant/i);expect(createService).not.toHaveBeenCalled();});
});
