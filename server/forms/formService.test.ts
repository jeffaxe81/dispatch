import { describe,expect,it,vi } from "vitest"; import { createFormService,type FormServicePorts } from "./formService";
const publishedDefinition={schemaVersion:1 as const,title:"Vistoria",fields:[{id:"notes",key:"notes",label:"Observações",type:"short_text" as const,required:true}]};
function ports():FormServicePorts{return{repository:{getTemplate:vi.fn(async id=>({id,tenantId:7,status:"draft" as const})),listTemplates:vi.fn(async()=>[{id:3,tenantId:7,status:"active" as const}]),getVersion:vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"draft" as const,definition:{schemaVersion:1,title:"F",fields:[]}})),listVersions:vi.fn(async()=>[]),getSubmission:vi.fn(async id=>({id,tenantId:7,formId:3,formVersionId:5,revision:1,status:"submitted" as const,answers:{notes:"Antes"}})),createDraft:vi.fn(async input=>({id:3,...input})),saveDraft:vi.fn(async input=>input),publishVersion:vi.fn(async input=>input),activateForm:vi.fn(async input=>input),disableForm:vi.fn(async input=>input),createSubmission:vi.fn(async input=>({id:21,...input})),appendRevision:vi.fn(async input=>({id:31,...input})),bindForm:vi.fn(async input=>({id:41,...input})),listSubmissionsForIncident:vi.fn(async()=>[{id:21,tenantId:7,status:"submitted"}])},audit:{append:vi.fn(async()=>undefined)},events:{append:vi.fn(async()=>undefined)}};}
describe("D-008 application service",()=>{
it("lista e consulta formulários no repository tenant-scoped",async()=>{const p=ports();const s=createFormService(7,p);expect(await s.list()).toHaveLength(1);expect(await s.get({formId:3})).toEqual(expect.objectContaining({id:3,tenantId:7}));expect(p.repository.listTemplates).toHaveBeenCalled();});
it("cria rascunho e audita criação",async()=>{const p=ports();const result=await createFormService(7,p).createDraft({code:"VISTORIA",name:"Vistoria",actorUserId:9,now:new Date()});expect(p.repository.createDraft).toHaveBeenCalledWith({code:"VISTORIA",name:"Vistoria",createdByUserId:9});expect(p.audit.append).toHaveBeenCalledWith(expect.objectContaining({action:"create_draft"}));expect(result).toEqual(expect.objectContaining({id:3}));});
it("atualiza somente versão draft do mesmo tenant",async()=>{const p=ports();const definition={schemaVersion:1 as const,title:"Nova",fields:[]};await createFormService(7,p).updateDraft({versionId:5,definition,actorUserId:9,now:new Date()});expect(p.repository.saveDraft).toHaveBeenCalledWith({versionId:5,definition,actorUserId:9});p.repository.getVersion=vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"published" as const,definition}));await expect(createFormService(7,p).updateDraft({versionId:5,definition,actorUserId:9,now:new Date()})).rejects.toThrow(/imutáveis|rascunho/i);});
it("consulta submissões da ocorrência sem solicitar transição",async()=>{const p=ports();const result=await createFormService(7,p).forIncident({incidentId:"88"});expect(p.repository.listSubmissionsForIncident).toHaveBeenCalledWith("88");expect(result.incidentTransitionRequested).toBe(false);});
it("publica versão e ativa template com auditoria e evento",async()=>{const p=ports();const result=await createFormService(7,p).publishFormVersion({versionId:5,actorUserId:9,now:new Date()});expect(p.repository.publishVersion).toHaveBeenCalled();expect(result.versionStatus).toBe("published");});
it("inicia submissão somente em versão publicada, sem marcar envio",async()=>{const p=ports();p.repository.getVersion=vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"published" as const,definition:publishedDefinition}));const result=await createFormService(7,p).startSubmission({formId:3,formVersionId:5,actorUserId:9,now:new Date()});expect(p.repository.createSubmission).toHaveBeenCalledWith(expect.objectContaining({status:"in_progress",answers:{}}));expect(p.events.append).toHaveBeenCalledWith(expect.objectContaining({eventType:"submission.started"}));expect(result.incidentTransitionRequested).toBe(false);});
it("submissão valida respostas e não transiciona ocorrência",async()=>{const p=ports();p.repository.getVersion=vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"published" as const,definition:publishedDefinition}));const result=await createFormService(7,p).submitForm({formId:3,formVersionId:5,actorUserId:9,answers:{notes:"OK"},now:new Date()});expect(p.repository.createSubmission).toHaveBeenCalledWith(expect.objectContaining({status:"submitted"}));expect(result.incidentTransitionRequested).toBe(false);});
it("corrige por nova revisão",async()=>{const p=ports();p.repository.getVersion=vi.fn(async id=>({id,tenantId:7,formId:3,version:1,status:"published" as const,definition:publishedDefinition}));await createFormService(7,p).correctSubmission({submissionId:21,actorUserId:9,answers:{notes:"Depois"},reason:"Correção",now:new Date()});expect(p.repository.appendRevision).toHaveBeenCalledWith(expect.objectContaining({revision:2,submissionStatus:"corrected"}));});
it("desativa sem apagar histórico",async()=>{const p=ports();p.repository.getTemplate=vi.fn(async id=>({id,tenantId:7,status:"active" as const}));expect((await createFormService(7,p).disableForm({formId:3,actorUserId:9,now:new Date()})).status).toBe("disabled");});
it("binding exige versão publicada explícita",async()=>{const p=ports();p.repository.getVersion=vi.fn(async id=>({id,tenantId:7,formId:3,version:2,status:"published" as const,definition:publishedDefinition}));await createFormService(7,p).bindForm({formId:3,formVersionId:5,contextType:"incident",contextId:"88",actorUserId:9,now:new Date()});expect(p.repository.bindForm).toHaveBeenCalledWith(expect.objectContaining({formVersionId:5}));});
});
