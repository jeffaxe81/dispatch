# CP-016 — preparação documental e revisão

Data: 05/09/2026. Status: preparação local, sem commit ou publicação deste documento.

## Referências verificadas

- PR: https://github.com/jeffaxe81/dispatch/pull/22 — aberto, Draft, não mesclado.
- Branch: `feature/cp016-operational-data-model`.
- HEAD remoto consultado: `9b563f081c1b8b95bd9b939dea179eefa66ba265`.
- HEAD local inspecionado: `353d3d2b01de6648c212687319ec7927a8df36ee`; árvore `81e47b1d171b80203c6c83fd8d868eeb0d6728b1` antes desta preparação documental.
- CI consultado: https://github.com/jeffaxe81/dispatch/actions/runs/33958882591 — job `101287090298` concluído com sucesso; segurança, TypeScript, testes, build e geração de migration reportados como sucesso.
- Essa evidência é do CI existente, não de uma nova execução local nem dos documentos agora preparados. Gerar migration não comprova aplicação em banco real.
- `package.json` contém versão `1.15.5`, preservada. A versão `1.0.0` pertence exclusivamente ao adendo AXE-PM-AD-001.

## Pendências de revisão

1. **Cobertura transacional insuficiente para encerrar a jornada auditável.** `server/workSessions.test.ts` contém apenas dois testes da normalização do motivo; não exercita `adjustWorkSession` nem comprova persistência/rollback de sessão, evento e auditoria. Exigir testes transacionais pertinentes antes de declarar a funcionalidade homologada.
2. **Integração da elegibilidade ainda não demonstrada.** A busca em `server` encontra `evaluateDispatchEligibility` na definição e nos testes, e `getEligibleTeamCandidates` apenas na definição. Não apresentar a regra pura como proteção já conectada ao fluxo de despacho. Confirmar a etapa de integração prevista no escopo.
3. **Ajuste de datas precisa de validação e decisão funcional.** `adjustWorkSession` permite atualizar início/fim sem conferir a ordem cronológica e não altera o status da sessão. Definir invariantes, testar datas inconsistentes e decidir a sincronização necessária antes de expor a operação. Não foi reproduzido um incidente em banco real.
4. **Concorrência da presença requer teste específico.** `syncOperationalPresenceTx` faz leitura seguida de atualização/inserção; o schema de `operational_presence` tem índice não único por equipe. Investigar concorrência entre os chamadores e comprovar a unicidade lógica; a leitura estática, isoladamente, não prova ocorrência de duplicatas.
5. **Descrição do PR desatualizada.** O texto ainda diz que a base temporária é `main`, mas a base consultada é `integration/cp016-validation-base-20260903`. Preparar correção documental sem alterar a base, marcar o PR como pronto ou autorizar merge.
6. **Plano não equivale a conclusão.** Permanecem necessárias evidências para aplicação da migration em ambiente autorizado, trilhas, configuração NEO, reset e homologação. Não marcar as dez tarefas concluídas por causa de um CI verde.

## Preparação para Git

- Adendo: `docs/governance/AXE-PM-AD-001-agentes-paralelos.md`.
- Revisão: este arquivo.
- Mensagem proposta: `docs: record parallel-agent approval policy and CP016 review`.
- Escopo: apenas documentação; não alterar versão do aplicativo.
- Antes de publicar: conferir as diferenças, integridade documental, HEAD remoto atualizado e autorização específica. Não sobrescrever o remoto a partir do histórico local divergente e não usar force push.
- Após publicação autorizada: verificar SHA remoto e CI correspondente. Qualquer correção executável exige seus próprios testes e revisão antes da aprovação.
- Sem merge, deploy, alteração de `main` ou aprovação global do CP-016.

O adendo continua independente: a fonte canônica do Prompt Master ainda precisa ser identificada para incorporação sem inventar sua versão histórica.
