# D-011B.4 — Recovery Executor Safety Boundary — Verification Evidence

Status: CANDIDATO TÉCNICO / PR DRAFT — MERGE NÃO AUTORIZADO
Date: 2026-09-08
PR: #59
Baseline: `main` @ `41ce6f3cc7d07a28573d594f8a87ab8c7c732c45`
Verified functional head: `c9cca9465684599ff2d39a443af454022d81c76b`

## 1. Scope verified

D-011B.4 introduces a fail-closed execution boundary after the recovery coordinator while preserving a strictly simulation-only runtime.

Verified safety properties in the candidate:
- no real restart/process/container/orchestrator execution adapter;
- executor capability restricted to `simulation` / `noop`;
- `real` and unknown execution capabilities rejected;
- authorization and kill-switch checks remain fail-closed;
- reservation/action/tenant/lease ownership bindings are validated;
- fencing is revalidated immediately before executor invocation;
- persistent execution transition is monotonic and compare-and-set based;
- duplicate execution cannot invoke the simulated executor twice;
- timeout and cooperative cancellation terminalize deterministically;
- late executor completion cannot overwrite a terminal timeout/cancel state;
- executor exceptions/results are sanitized and invalid/mismatched outcomes become terminal `unknown_outcome` semantics;
- audit output is sanitized;
- runtime composition wires only the simulated adapter through the execution boundary;
- no endpoint/UI/CLI or production knob enabling real recovery is introduced.

## 2. Fresh CI evidence for functional head

GitHub Actions — `Qualidade` run `34286113899`, job `102261994829`, checked out exact head `c9cca9465684599ff2d39a443af454022d81c76b`.

Commands executed by CI:

```sh
pnpm security:check
pnpm check
pnpm test
pnpm build
```

Results:
- `pnpm security:check`: GREEN — `8 migrações, 22 correções/invariantes preservadas e D-010B/D-010C protegidos`;
- `pnpm check`: GREEN — `tsc --noEmit` completed successfully;
- `pnpm test`: GREEN — **226/226 test files, 979/979 tests, 0 failures**;
- `pnpm build`: GREEN — Vite and esbuild completed successfully.

Additional workflows on the same head:
- GIS visual homologation run `34286113880`: GREEN;
- NEO external compatibility run `34286113886`: GREEN;
- NEO workspace visual homologation run `34286114225`: GREEN.

## 3. D-011B.4 focused evidence present in the full suite

The GREEN suite includes, among others:
- `recoveryExecutionBoundary.test.ts`;
- `recoveryExecutionBoundary.guardFailure.test.ts`;
- `recoveryExecutionBoundary.internalFailure.test.ts`;
- `recoveryExecutionBoundary.resultBinding.test.ts`;
- `recoveryExecutionBoundary.resultSemantics.test.ts`;
- `recoveryExecutionSafetyGuard.test.ts`;
- `recoveryExecution.test.ts`;
- `recoveryExecutionAudit.test.ts`;
- `recoveryActionRecord.execution.test.ts`;
- `recoveryActionRecord.cas.test.ts`;
- `activeRecoveryBootstrap.test.ts`;
- `activeRecoveryBootstrap.cancellation.test.ts`;
- `d011b4SafetyBoundary.test.ts`;
- D-011B.1, D-011B.2 and D-011B.3 safety/regression tests.

## 4. Non-blocking warnings observed

- Vite analytics environment variables are not defined in CI;
- existing Leaflet dynamic/static import warning;
- frontend chunk-size warning;
- one `TimeoutOverflowWarning` was emitted during `activeRecoveryBootstrap.test.ts`; the test and full suite still completed GREEN. This warning must not be interpreted as a failed timeout/cancellation assertion, but should be reviewed if timing fixtures are changed later.

## 5. Gates that remain mandatory before approval or merge

This report does **not** authorize promotion from Draft or merge.

Before any approval/merge decision:
1. validate the new documentation head again through the same CI workflows because this evidence file changes the branch SHA;
2. perform final PR diff review specifically for forbidden execution imports, accidental real-side-effect paths, secret/stack leakage, weakened D-011B.3 controls, and replay/timeout/cancellation regressions;
3. require zero Critical or Important review findings;
4. confirm PR remains simulation-only and fail-closed;
5. obtain explicit project-owner authorization before changing Draft/Ready state or merging to `main`.

Explicitly prohibited in this cycle:
- real restart or process termination;
- systemd/service-manager execution;
- Docker/Podman/Kubernetes/SSH/hypervisor/cloud execution;
- database failover/restore/migration;
- automatic rollback;
- deploy or production enablement;
- automatic merge.

## 6. Next step

Validate the documentation head generated by this commit, then perform final focused diff review. Keep PR #59 Draft until both are clean and explicit approval is granted.
